"use strict";

// Minimal compatibility shim for community-cli-plugin's expected util
// The real project expects a `styleText` helper; implement a no-op
// that returns the text unchanged to avoid runtime errors.

function styleText(_style, text) {
  // style may be string or array; we intentionally ignore styling in this shim
  return text;
}

module.exports = {
  styleText,
};
